# Local gate for submitted bot.py (challenge TOML may differ).
line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[lint.per-file-ignores]
# PYTHONPATH bootstrap must precede image-baked imports.
"bot.py" = ["E402"]
